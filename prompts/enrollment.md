# Enrollment prompt card

The candidate phase is complete. Read the uploaded authorization, runbook, and provisioning JSON. Download bootstrap only from the exact `bootstrap.url` in the provisioning JSON, inspect it, and verify its SHA-256. The upload package intentionally does not contain a `.py` bootstrap because Arena does not support direct Python-file uploads. If scope and expiry match, use the one-time invite to request enrollment without revealing it. If pending, report provisioning ID, request ID, workspace, privileges, and stop for local operator approval. After approval, complete only read-only verification.
