# Controlling Operator Confirmation

I am the Primary Principal Operator controlling this Conduit deployment. I explicitly authorize the recipient to act under the attached provisioning ID and its declared `authorization.accessClass`.

The recipient may contact only the declared server, download and verify only the declared bootstrap, and submit exactly one enrollment request before the absolute ISO-8601 expiry instant. Only the envelope's exact privileges and workspaces are authorized. No undeclared mutation, execution, administration, system operation, commit, push, or workspace access is authorized.

If pending, report the request ID without revealing secrets and wait or safely resume the same bootstrap request. Stop on any URL, digest, absolute-time expiry, grant, workspace, or filesystem-permission mismatch.
