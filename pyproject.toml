[project]
name = "conduit-arena-admission"
version = "1.0.0"
requires-python = ">=3.10"
description = "Public Arena agent candidate assessment and Conduit provisioning schemas"

[tool.pytest.ini_options]
testpaths = ["tests"]
